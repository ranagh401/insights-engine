"""Minimal compatibility layer for the Renuity insights portal replica (Power BI approach).

The production service depends on the internal ``oneplatform_core`` package. This folder
vendors only the symbols and behaviors referenced by ``ms_renuity_insights_portal`` so
the same application code can run in isolation.
"""
