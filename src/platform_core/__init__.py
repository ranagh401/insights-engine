"""Minimal compatibility layer for the Client insights portal replica (Power BI approach).

The production service depends on the internal ``platform_core`` package. This folder
vendors only the symbols and behaviors referenced by ``insights_engine`` so
the same application code can run in isolation.
"""
